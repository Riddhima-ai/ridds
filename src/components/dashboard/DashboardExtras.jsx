import { useMemo, useState } from "react";
import { CalendarDays, ChevronLeft, ChevronRight, Megaphone, Plus, Trash2 } from "lucide-react";
const startingAnnouncements=[{id:"a1",title:"Design review is scheduled for Friday",time:"2h ago"},{id:"a2",title:"Flight test checklist updated",time:"Yesterday"}];
function monthDays(cursor){const start=new Date(cursor.getFullYear(),cursor.getMonth(),1);const lead=start.getDay();const total=new Date(cursor.getFullYear(),cursor.getMonth()+1,0).getDate();return Array.from({length:42},(_,i)=>i-lead+1>0&&i-lead+1<=total?i-lead+1:null)}
export default function DashboardExtras(){const [cursor,setCursor]=useState(new Date(2025,5,1));const [selected,setSelected]=useState(4);const [events,setEvents]=useState({4:"Design review",12:"Sensor calibration",18:"Team sync"});const [announcements,setAnnouncements]=useState(startingAnnouncements);const [newAnnouncement,setNewAnnouncement]=useState("");const [newEvent,setNewEvent]=useState("");const days=useMemo(()=>monthDays(cursor),[cursor]);const label=cursor.toLocaleDateString("en-US",{month:"long",year:"numeric"});const addAnnouncement=e=>{e.preventDefault();if(!newAnnouncement.trim())return;setAnnouncements(a=>[{id:crypto.randomUUID(),title:newAnnouncement.trim(),time:"Just now"},...a]);setNewAnnouncement("")};const addEvent=e=>{e.preventDefault();if(!newEvent.trim())return;setEvents(x=>({...x,[selected]:newEvent.trim()}));setNewEvent("")};return <section className="workspace-grid" id="announcements"><section className="workspace-card calendar-workspace"><header><h2><CalendarDays/> Calendar</h2><div><button onClick={()=>setCursor(new Date(cursor.getFullYear(),cursor.getMonth()-1,1))} aria-label="Previous month"><ChevronLeft/></button><strong>{label}</strong><button onClick={()=>setCursor(new Date(cursor.getFullYear(),cursor.getMonth()+1,1))} aria-label="Next month"><ChevronRight/></button></div></header><div className="calendar-weekdays">{["Sun","Mon","Tue","Wed","Thu","Fri","Sat"].map(x=><span key={x}>{x}</span>)}</div><div className="calendar-cells">{days.map((day,i)=><button key={i} className={`calendar-cell ${day===selected?"selected":""}`} disabled={!day} onClick={()=>setSelected(day)}>{day&&<><b>{day}</b>{events[day]&&<small>{events[day]}</small>}</>}</button>)}</div><form className="calendar-add" onSubmit={addEvent}><span>Selected: {selected} {label}</span><input value={newEvent} onChange={e=>setNewEvent(e.target.value)} placeholder="Add calendar task"/><button type="submit">Add</button></form></section><section className="workspace-card announcement-workspace"><header><h2><Megaphone/> Announcements</h2><span>{announcements.length} updates</span></header><form className="announcement-add" onSubmit={addAnnouncement}><input value={newAnnouncement} onChange={e=>setNewAnnouncement(e.target.value)} placeholder="Write an announcement"/><button type="submit" aria-label="Add announcement"><Plus/></button></form><div className="workspace-announcement-list">{announcements.map(item=><article key={item.id}><i/><div><strong>{item.title}</strong><small>{item.time}</small></div><button onClick={()=>setAnnouncements(items=>items.filter(a=>a.id!==item.id))} aria-label={`Delete ${item.title}`}><Trash2/></button></article>)}</div></section></section>}